#include "main.h"
/**
 * print_numbers - writes a character to the standard output (stdout)
 * @c: the character to write
 *
 * Return: on success, the character written, or -1 on error
 */
void print_numbers(void)

{
int x;
	
for (x = '0'; x <= '9'; x++)
{
_putchar(x);
}
_putchar('\n');
}
