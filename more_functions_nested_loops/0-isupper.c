#include "main.h"
/**
 *_isupper  - It checks whether the integer represents an uppercase character
 *@c: return zero
 */
int _isupper(int c)
{
if (c >= 'A' && c <= 'Z')
return (1);
else
return (0);
}
