/* To compile: gcc c.c -o c -Wall */
#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
printf("This program prints exactly, with proper grammar, but the outcome is a piece of art,");
return (0);
}
